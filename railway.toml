[build]
builder = "DOCKERFILE"
dockerfilePath = "railway-bulletproof-v2.Dockerfile"

[deploy]
startCommand = "node emergency.js"
healthcheckPath = "/ping"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[environments]
  [environments.production]
    startCommand = "node emergency.js"