[build]
buildCommand = "npm run build"
startCommand = "npm run start"
nixpacksPlan.phases.setup.nixPkgs = ["nodejs_18"]

[deploy]
healthcheckPath = "/ping"
healthcheckTimeout = 60000
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[resources]
memoryLimit = 512
cpuLimit = 0.5
